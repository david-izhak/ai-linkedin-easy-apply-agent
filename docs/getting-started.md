# 🚀 Руководство по началу работы

Это подробное руководство поможет вам установить, настроить и запустить **LinkedIn Easy Apply Bot** для автоматизации поиска работы и подачи заявок.

## 📋 Содержание

1. [Установка и зависимости](#установка-и-зависимости)
2. [Настройка конфигурации](#настройка-конфигурации)
3. [Быстрый запуск](#быстрый-запуск)
4. [Поэтапный запуск (рекомендуется)](#поэтапный-запуск)
5. [Мониторинг работы](#мониторинг-работы)
6. [Устранение неполадок](#устранение-неполадок)

---

## Установка и зависимости

### Предварительные требования

- **Python 3.7+**: Убедитесь, что Python установлен
- **LinkedIn аккаунт**: Действующий аккаунт LinkedIn

### Шаги установки

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd linkedin-easy-apply-bot
   ```

2. **Создайте и активируйте виртуальное окружение:**
   ```bash
   # Windows
   python -m venv venv
   venv\Scripts\activate

   # macOS/Linux
   python -m venv venv
   source venv/bin/activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install uv
   uv pip install -r requirements.txt
   ```
   *Примечание: Если `requirements.txt` отсутствует, используйте `pyproject.toml`*

4. **Установите браузеры для Playwright:**
   ```bash
   playwright install
   ```

---

## Настройка конфигурации

Конфигурация бота разделена на два файла:

### 📄 config.py

Основной файл конфигурации с настройками работы бота.

#### 1. Учетные данные LinkedIn
```python
LINKEDIN_EMAIL = "your_email@gmail.com"
LINKEDIN_PASSWORD = "your_password"
```

#### 2. Параметры поиска работы
```python
KEYWORDS = "Software Engineer"      # Ключевые слова для поиска
GEO_ID = "118490091"                # Geo ID региона
DISTANCE = "20"                      # Радиус поиска (мили/км)
SORT_BY = "DD"                       # DD - по дате, R - по релевантности
```

#### 3. Период поиска вакансий
```python
JOB_SEARCH_PERIOD_SECONDS = 1728000  # 20 дней (по умолчанию)
```

**Что это:** Определяет, за какой период времени искать вакансии на LinkedIn.

**Таблица конвертации:**
| Период | Секунды | Настройка |
|--------|---------|-----------|
| 1 день | 86400 | `JOB_SEARCH_PERIOD_SECONDS = 86400` |
| 7 дней | 604800 | `JOB_SEARCH_PERIOD_SECONDS = 604800` |
| 14 дней | 1209600 | `JOB_SEARCH_PERIOD_SECONDS = 1209600` |
| 20 дней (default) | 1728000 | `JOB_SEARCH_PERIOD_SECONDS = 1728000` |
| 30 дней | 2592000 | `JOB_SEARCH_PERIOD_SECONDS = 2592000` |

**Рекомендации:**
- **Ежедневные запуски:** 1-2 дня (86400-172800 сек)
- **Еженедельные запуски:** 7-10 дней (604800-864000 сек)
- **Редкие запуски:** 20-30 дней (1728000-2592000 сек)

**Формула:** `Секунды = Дни × 86400`

⚠️ **Важно:** LinkedIn обычно показывает вакансии возрастом до 30-60 дней. Периоды более 90 дней могут давать меньше результатов.

#### 4. Персональные данные
```python
PHONE = "ваш_телефон"
CV_PATH = "путь/к/вашему/резюме.pdf"
HOME_CITY = "Ваш город"
YEARS_OF_EXPERIENCE = {
    "default": 3,
    # ... другие настройки
}
```

#### 5. Лимиты для тестирования
```python
MAX_JOBS_TO_DISCOVER = 10     # Максимум вакансий для поиска
MAX_JOBS_TO_ENRICH = 5        # Максимум вакансий для обогащения
MAX_APPLICATIONS_PER_DAY = 3  # Дневной лимит заявок
WAIT_BETWEEN_APPLICATIONS = 30000  # 30 секунд между заявками
```

⚠️ **НАЧНИТЕ С МАЛЫХ ЧИСЕЛ** для первого тестового запуска!

#### 6. Режим работы бота
```python
BOT_MODE = "discovery"  # discovery, enrichment, processing, full_run
BROWSER_HEADLESS = False  # False - видимый браузер, True - фоновый режим
```

### 📄 .env файл (для LLM)

Для интеграции с LLM создайте файл `.env` в корне проекта:

```env
LLM_PROVIDER="openai"
LLM_MODEL="gpt-4-turbo"
LLM_API_KEY="sk-..."
LLM_THRESHOLD_PERCENTAGE=70
RESUME_TXT_PATH="resume.txt"
LLM_TIMEOUT=60
LLM_MAX_RETRIES=3
```

**Поддерживаемые провайдеры:**
- `openai` - OpenAI GPT модели
- `ollama` - Локальные модели через Ollama
- `anthropic` - Anthropic Claude модели

---

## Быстрый запуск

### Вариант 1: Минимальный тест (5-10 минут)

Для самого первого запуска:

1. **Discovery** с 5 вакансиями:
   ```python
   # В config.py
   BOT_MODE = "discovery"
   MAX_JOBS_TO_DISCOVER = 5
   ```
   ```bash
   python main.py
   ```

2. **Enrichment** для 3 вакансий:
   ```python
   # В config.py
   BOT_MODE = "enrichment"
   MAX_JOBS_TO_ENRICH = 3
   ```
   ```bash
   python main.py
   ```

3. **Processing** БЕЗ отправки (DRY RUN):
   ```python
   # В config.py
   BOT_MODE = "processing"
   MAX_APPLICATIONS_PER_DAY = 1
   ```
   ```bash
   python main.py  # БЕЗ SUBMIT!
   ```

4. **Processing** С отправкой (осторожно!):
   ```bash
   python main.py SUBMIT
   ```

### Вариант 2: Полный запуск

Когда будете готовы к автоматизации:

```python
# В config.py
BOT_MODE = "full_run"
MAX_JOBS_TO_DISCOVER = 10
MAX_JOBS_TO_ENRICH = 5
MAX_APPLICATIONS_PER_DAY = 3
```

```bash
# Тестовый запуск БЕЗ отправки
python main.py

# Реальный запуск С отправкой
python main.py SUBMIT
```

---

## Поэтапный запуск

### Этап 1: Discovery (Поиск вакансий) 🔍

**Настройка:**
```python
BOT_MODE = "discovery"
MAX_JOBS_TO_DISCOVER = 10
```

**Запуск:**
```bash
python main.py
```

**Что произойдет:**
- ✅ Бот откроет браузер
- ✅ Войдет в LinkedIn
- ✅ Найдет до 10 вакансий
- ✅ Сохранит их в базу данных (`jobs.db`)
- ❌ **НЕ будет** подавать заявки

**Проверка результатов:**
```sql
-- Откройте jobs.db в DB Browser for SQLite
SELECT * FROM vacancies WHERE status = 'discovered' ORDER BY created_at DESC;
```

---

### Этап 2: Enrichment (Обогащение данных) 📊

**Настройка:**
```python
BOT_MODE = "enrichment"
MAX_JOBS_TO_ENRICH = 5
```

**Запуск:**
```bash
python main.py
```

**Что произойдет:**
- ✅ Бот откроет страницы найденных вакансий
- ✅ Соберет детальную информацию (описание, требования)
- ✅ Сохранит в базу данных
- ❌ **НЕ будет** подавать заявки

**Проверка результатов:**
```sql
SELECT * FROM vacancies WHERE status = 'enriched';
```

---

### Этап 3: Processing (Обработка и заявки) 📝

⚠️ **ВАЖНО**: На этом этапе начинается реальное взаимодействие с формами!

#### 3a. Тестовый запуск БЕЗ отправки (рекомендуется)

**Настройка:**
```python
BOT_MODE = "processing"
MAX_APPLICATIONS_PER_DAY = 2
```

**Запуск:**
```bash
python main.py  # БЕЗ параметра SUBMIT
```

**Что произойдет:**
- ✅ Бот откроет формы заявок
- ✅ Заполнит все поля
- ✅ Вы увидите, как он работает
- ❌ **НЕ ОТПРАВИТ** заявки

#### 3b. Реальный запуск С отправкой

⚠️ **Осторожно**: Это отправит реальные заявки!

**Настройка:**
```python
MAX_APPLICATIONS_PER_DAY = 2  # НАЧНИТЕ С МАЛОГО!
```

**Запуск:**
```bash
python main.py SUBMIT
```

**Что произойдет:**
- ✅ Бот заполнит и **ОТПРАВИТ** заявки
- ✅ Вакансии получат ваше резюме

---

## Мониторинг работы

### Логи

Все действия бота записываются в консоль. Следите за сообщениями:

- ✅ `INFO` - нормальный процесс
- ⚠️ `WARNING` - проблемы, которые бот обработал
- ❌ `ERROR` - ошибки, требующие внимания

### База данных

Используйте DB Browser for SQLite для просмотра `jobs.db`:

```sql
-- Все вакансии
SELECT * FROM vacancies ORDER BY created_at DESC;

-- Только обогащенные
SELECT * FROM vacancies WHERE status = 'enriched';

-- Успешно поданные заявки
SELECT * FROM vacancies WHERE status = 'applied';

-- Пропущенные фильтром
SELECT * FROM vacancies WHERE status = 'skipped';
```

---

## Устранение неполадок

### Бот не находит вакансии

**Решение:**
- Проверьте `KEYWORDS`, `GEO_ID` в `config.py`
- Попробуйте увеличить период поиска:
  ```python
  JOB_SEARCH_PERIOD_SECONDS = 2592000  # 30 дней
  ```

### Ошибки при заполнении форм

**Решение:**
- Проверьте, что `CV_PATH` указывает на существующий файл
- Убедитесь, что `PHONE` указан правильно
- Проверьте `YEARS_OF_EXPERIENCE`

### Бот не отправляет заявки

**Решение:**
- Убедитесь, что используете `python main.py SUBMIT`
- Проверьте, что прошли все фазы:
  - Discovery → Enrichment → Processing

### LinkedIn требует CAPTCHA / 2FA

**Решение:**
- При первом запуске это нормально
- Бот приостановит выполнение
- Вручную пройдите проверку в открытом окне браузера
- Бот продолжит работу автоматически

---

## 📝 Checklist перед первым запуском

- [ ] Настроены учетные данные LinkedIn (`LINKEDIN_EMAIL`, `LINKEDIN_PASSWORD`)
- [ ] Указан путь к резюме (`CV_PATH`)
- [ ] Указан телефон (`PHONE`)
- [ ] Указан город (`HOME_CITY`)
- [ ] Установлены малые лимиты для тестирования
- [ ] Выбран режим `BOT_MODE = "discovery"` для первого запуска
- [ ] Понимаете, что делает каждый этап
- [ ] Готовы к ручному вмешательству при CAPTCHA/2FA

---

## 🛡️ Меры безопасности

1. **Всегда начинайте с малых чисел:**
   ```python
   MAX_JOBS_TO_DISCOVER = 5-10
   MAX_JOBS_TO_ENRICH = 3-5
   MAX_APPLICATIONS_PER_DAY = 1-3
   ```

2. **Сначала тестируйте БЕЗ параметра `SUBMIT`**

3. **Проверяйте логи** на наличие ошибок

4. **Мониторьте LinkedIn** вручную после первых запусков

5. **Делайте паузы** между запусками (не запускайте слишком часто)

---

## 🎉 Готово!

После успешного тестирования поэтапно, увеличьте лимиты и используйте режим `full_run` для автоматизации процесса поиска работы.

**Удачи в поиске работы! 🚀**

---

## Дополнительная информация

- [Архитектура проекта](architecture.md)
- [Описание компонентов](components.md)
- [Workflow и поток данных](workflow.md)
- [Схема базы данных](database-schema.md)
- [Интеграция с LLM](llm-integration.md)
- [Оптимизация производительности](performance-optimization.md)
- [Тестирование](testing.md)
- [Руководство по контрибьюции](contribution-guide.md)
